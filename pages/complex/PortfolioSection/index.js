import { Component } from "react";
import $ from "./style.css";

export default () => (
	<b className={$.container}>
		<b className={$.heading}>Portfolio</b>
		<b className={$.separator} />
		<b className={$.items}>
			<b className={$.item}> </b>
		</b>
	</b>
);
